{
  "name": "aironi-e-volatili",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "parcel": "^2.16.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router": "^7.9.4",
    "sharp": "^0.34.4",
    "yet-another-react-lightbox": "^3.25.0"
  },
  "scripts": {
    "dev": "parcel src/index.html --open",
    "prebuild": "node src/generate-thumbnails.js",
    "build": "parcel build src/index.html --public-url ./",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dotfiles --message \"Deploy $(date)\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
